data_dir = "data"

# Optional: enable NATS JetStream publishing (omit section to disable)
# [nats]
# url = "nats://127.0.0.1:4222"
# enabled = true

[[connections]]
name     = "binance_spot"
exchange = "binance_spot"
symbols  = ["ETHUSDT", "BTCUSDT", "BNBUSDT"]
depth_ms = 100

[[connections]]
name     = "binance_perp"
exchange = "binance_perp"
symbols  = ["ETHUSDT", "BTCUSDT", "BNBUSDT"]
depth_ms = 100
