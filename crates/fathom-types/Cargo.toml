[package]
name = "fathom-types"
version = "0.1.0"
edition = "2024"
description = "Shared types for Fathom L2 order book data"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"
thiserror = "2.0"
